# Security Note

This repository intentionally does not include live OpenCode configuration,
provider credentials, model routing files, local vault paths, or private
machine paths. Copy examples before use and replace placeholders with values
managed by your own OpenCode installation.
